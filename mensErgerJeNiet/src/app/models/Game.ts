/*
export class Game {
  element: any;
  canvas: any;
  ctx: any;

  constructor(config) {
    this.element = config.element;
    this.canvas = this.element.querySelector("game-canvas");
    this.ctx = this.canvas.getContext("2d");
  }
  init(){
    const pawn  = new Image();
    pawn.onload = () => {
  this.ctx.drawImage(pawn, 0, 0)
    };
    pawn.src = "./assets/images/pawn.png"
  }
}

*/
